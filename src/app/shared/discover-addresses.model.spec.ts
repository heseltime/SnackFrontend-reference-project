import { DiscoverAddresses } from './discover-addresses.model';

describe('DiscoverAddresses', () => {
  it('should create an instance', () => {
    expect(new DiscoverAddresses()).toBeTruthy();
  });
});

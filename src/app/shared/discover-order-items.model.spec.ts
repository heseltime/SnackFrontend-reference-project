import { DiscoverOrderItems } from './discover-order-items.model';

describe('DiscoverOrderItems', () => {
  it('should create an instance', () => {
    expect(new DiscoverOrderItems()).toBeTruthy();
  });
});
